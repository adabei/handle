<UserControl x:Class="Handle.WPF.NetworkSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      xmlns:cal="http://www.caliburnproject.org">

    <Grid>
      <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
      <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" FontSize="22">network list</TextBlock>
<StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Left">
        <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      
      <Label Grid.Column="0" Grid.Row="0">Nick name:</Label>
        <TextBox Grid.Column="1" Grid.Row="0" x:Name="GlobalIdentity_Name" Height="20" Width="75"/>
      
      <Label Grid.Column="0" Grid.Row="1">Alternative:</Label>
      <TextBox Grid.Column="1" Grid.Row="1" x:Name="GlobalIdentity_Alternative" Height="20" Width="75"/>
 
      <Label Grid.Column="0" Grid.Row="2">Real name:</Label>
      <TextBox Grid.Column="1" Grid.Row="2" x:Name="GlobalIdentity_RealName" Height="20" Width="75"/>
    </Grid>
      <ListView x:Name="Networks" Width="150" Height="80">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding Name}"/>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackPanel>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
      <Button x:Name="Add" Content="Add" ToolTip="Add a new network" Width="Auto" Height="25"/>
      <Button x:Name="Edit" Content="Edit" ToolTip="Edit network" Width="Auto" Height="25"/>
      <Button x:Name="Remove" Content="Remove" ToolTip="Remove selected network(s)" Width="Auto" Height="25"/>
      <Button x:Name="Cancel" Content="Cancel" ToolTip="Cancel" Width="Auto" Height="25"/>
      <Button x:Name="Connect" Content="Connect" ToolTip="Connect" Width="Auto" Height="25"/>
    </StackPanel>
    
  </Grid>
</UserControl>